[package]
name = "tabless"
version = "0.1.0"
edition = "2024"

[dependencies]
url = "2.5"
rusqlite = { version = "0.32", features = ["bundled"] }
sublime_fuzzy = "0.7"
which = "7.0"

[dev-dependencies]
